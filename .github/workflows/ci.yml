name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # contracts 为 git+ssh 依赖（tag v0.2.0, subdirectory=gen/python）：装载只读 deploy key
      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.CONTRACTS_DEPLOY_KEY }}
      - run: ssh-keyscan github.com >> ~/.ssh/known_hosts 2>/dev/null
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv sync
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run lint-imports
      - run: uv run mypy
      - run: uv run pytest
